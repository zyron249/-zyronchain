name: Temporary Legacy CLI Recovery Patcher

on:
  pull_request:
    branches: [main]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout exact PR branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22.23.2
          cache: npm
          cache-dependency-path: l1/package-lock.json
      - name: Apply focused source/test/doc patch
        run: python3 l1/scripts/tmp-legacy-cli-recovery-patch.py
      - name: Install locked dependencies
        working-directory: l1
        run: npm ci
      - name: Typecheck, build, and run focused regressions
        working-directory: l1
        run: |
          npm run typecheck
          npm run build
          node --test dist/test/legacy-cli-recovery-custody.test.js dist/test/cli-recovery-file.test.js dist/test/cli-checkpoint-digest.test.js
      - name: Remove temporary patch machinery and commit final branch state
        env:
          GH_TOKEN: ${{ github.token }}
          HEAD_BRANCH: ${{ github.event.pull_request.head.ref }}
        run: |
          rm -f .github/workflows/tmp-legacy-cli-recovery-patch.yml l1/scripts/tmp-legacy-cli-recovery-patch.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --quiet && { echo 'No focused changes produced'; exit 1; }
          git commit -m "Harden direct legacy CLI recovery reads"
          git push "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" HEAD:${HEAD_BRANCH}
