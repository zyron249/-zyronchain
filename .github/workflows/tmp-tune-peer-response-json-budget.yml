name: Temporary Peer Response JSON Budget Tuner

on:
  push:
    branches:
      - agent/peer-response-json-parse-budget-v1

permissions:
  contents: write

jobs:
  tune:
    if: github.event.head_commit.message == 'Add temporary peer response parse budget tuning workflow'
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: agent/peer-response-json-parse-budget-v1
      - name: Tune peer response JSON parse accounting
        run: node l1/scripts/tune-peer-response-json-budget.mjs
      - name: Verify diff
        run: git diff --check
      - name: Remove temporary tuning machinery
        run: git rm l1/scripts/tune-peer-response-json-budget.mjs .github/workflows/tmp-tune-peer-response-json-budget.yml
      - name: Commit and push final tuning
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name 'Zyron Chain'
          git config user.email 'ciyagerkurdi@icloud.com'
          git add l1/src/node-base.ts docs/PEER_HTTP_JSON_PARSE_BUDGET.md
          git commit -m 'Tighten peer response decoded memory accounting'
          git remote set-url origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git push origin HEAD:agent/peer-response-json-parse-budget-v1
