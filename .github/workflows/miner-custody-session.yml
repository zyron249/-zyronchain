name: Miner Custody Session Identity CI

on:
  pull_request:
    paths:
      - 'l1/native/miner-custody-posix.c'
      - 'l1/native/miner-custody-posix-impl.inc'
      - 'l1/scripts/test-miner-custody-session-required.mjs'
      - 'l1/docs/security/miner-custody-session-identity-required.md'
      - '.github/workflows/miner-custody-session.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  session-identity:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-15]
        node: [22, 24]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    defaults:
      run:
        working-directory: l1
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: ${{ matrix.node }}
      - name: Check regression syntax
        run: node --check scripts/test-miner-custody-session-required.mjs
      - name: Reject unbound custody sessions
        run: node scripts/test-miner-custody-session-required.mjs
